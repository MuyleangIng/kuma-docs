# kuma-docs

Standalone documentation website for `koma-khqr`.

The markdown source used by the site is stored locally in `content/docs`, so the project can
deploy on Vercel without depending on a parent monorepo layout.

## Run

```bash
cd kuma-docs
npm install
npm run dev
```

Open `http://localhost:3000`.
